[workspace]
resolver = "2"
members = [
    "extend/lua",
    "extend/luakit",
    "extend/lstdfs",
    "extend/ljson",
    "core/quanta",
]

[profile.release]
# strip = true
# codegen-units = 1
# lto = true
# panic = "abort"