[workspace.lints.rust]
# unsafe_code = "forbid"
# unused = "allow" # For experimental dev.

[workspace]
resolver = "2"
members = [
    "extend/lua",
    "extend/ljson",
    "extend/lstdfs",
    "core/quanta",
]

[profile.release]
# strip = true
# codegen-units = 1
# lto = true
# panic = "abort"